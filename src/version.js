/**
 * Central application version.
 *
 * SemVer format: MAJOR.MINOR.PATCH
 * - MAJOR: incompatible changes
 * - MINOR: backward-compatible feature additions
 * - PATCH: backward-compatible fixes
 */
export const APP_VERSION = "0.1.0";

/**
 * Version bump guidance for contributors.
 * Keep this in sync with CONTRIBUTING.md and CHANGELOG.md entries.
 */
export const VERSION_BUMP_NOTE =
  "Bump APP_VERSION only when app behaviour changes; record the same version in CHANGELOG.md.";
